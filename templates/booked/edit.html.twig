{% extends 'base.html.twig' %}

{% block title %}Modifier une réservation{% endblock %}

{% block body %}
    <div class="backgroundImageContainer">
        <img src="{{ asset('img/homePage/home.png') }}" alt="salon" class="backgroundImage"/>
    </div>

    <div class="container-lg">
        <h1>{{ block('title') }}</h1>

        {{ include('booked/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a href="{{ path('app_booked_index') }}">Retour à la liste des réservations</a>

        {{ include('booked/_delete_form.html.twig') }}
    </div>
{% endblock %}
