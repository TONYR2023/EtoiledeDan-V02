<form method="post" action="{{ path('app_booked_delete', {'id': booked.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ booked.id) }}">
    <button class="btn btn-danger mt-3 mb-3">Supprime cette réservation</button>
</form>
